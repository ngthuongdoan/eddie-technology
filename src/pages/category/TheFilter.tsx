import React from 'react';
import ClassNameProps from '../../model/ClassNameProps';

interface Props {}

const TheFilter: React.FC<Props & ClassNameProps> = (props): JSX.Element => {
  return <div></div>;
};

export default TheFilter;
